% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{theme_sl}
\alias{theme_sl}
\title{Build a \code{bslib} theme from the Shelby Level brand}
\usage{
theme_sl(...)
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[bslib:bs_theme]{bslib::bs_theme()}}, letting
you override or extend brand defaults per-app if needed (e.g. a
one-off \code{bg}/\code{fg} tweak for a specific dashboard).}
}
\value{
A \code{bs_theme} object.
}
\description{
Drop-in replacement for \code{bslib::bs_theme(brand = "_brand.yml")} that
points at the package's bundled brand file instead of a local copy.
Pass this to the \code{theme} argument of any \code{bslib} page function
(\code{page_sidebar()}, \code{page_navbar()}, \code{fluidPage()}, etc.).
}
\examples{
\dontrun{
library(bslib)
ui <- page_sidebar(
  title = "Hockey R Learning Hub",
  theme = theme_sl(),
  sidebar = sidebar("..."),
  "..."
)
}
}
